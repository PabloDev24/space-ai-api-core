# Postgres local para desarrollo (docker-compose.dev.yml).
# Contenedor efímero solo-localhost con datos demo — no es un secreto real.
# Debe coincidir con el ConnectionStrings:DefaultConnection que uses vía `dotnet user-secrets`.
POSTGRES_DB=smartspaces
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
